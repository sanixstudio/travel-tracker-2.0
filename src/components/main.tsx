import MapBox from "./map-box";

const Main = () => {
  return (
    <div className="text-8xl w-full bg-teal-300 dark:bg-slate-800 h-screen">
      <MapBox />
    </div>
  );
};

export default Main;
